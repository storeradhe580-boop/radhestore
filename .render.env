# Render-specific environment variables
PORT=${PORT:-8000}
HOST=${HOST:-0.0.0.0}

# Laravel environment
APP_ENV=production
APP_DEBUG=false

# Export for child processes
export PORT
export HOST
export APP_ENV
export APP_DEBUG
